# FastAPI template without Docker

🤖 < In Da Local

## How to Use

1. cd ./backend && python3 -m venv venv && . ./venv/bin/activate && pip install -U pip setuptools wheel && pip install -r requirements && sh ./create_env.sh
2. DONE
